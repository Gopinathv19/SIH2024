import React from 'react';
import '../Style/Logout.css'; // Import CSS for Logout

const Logout = () => {
    return (
        <div className="logout">
            <h2>Logout</h2>
            <p>You have been logged out successfully.</p>
            {/* Add more content as needed */}
        </div>
    );
};

export default Logout;
